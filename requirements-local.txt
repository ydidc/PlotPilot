# ════════════════════════════════════════════
# aitext — 本地模型扩展依赖（~2GB）
# ════════════════════════════════════════════
#
# 用法: pip install -r requirements-local.txt
#
# 包含本地 Embedding 模型所需的大型依赖。
# 仅在需要使用「本地向量模式」时才安装。
#
# 安装前请确保磁盘有至少 5GB 可用空间。
# 首次安装可能需要 5~20 分钟（取决于网络速度）。
# ════════════════════════════════════════════

# ── 数值计算 ──
numpy>=1.21.0

# ── 向量索引 ──
faiss-cpu>=1.7.4

# ── 本地 Embedding 模型（含 PyTorch 依赖） ──
sentence-transformers>=2.2.0
